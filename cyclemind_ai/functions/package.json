{
  "name": "cyclemind-functions",
  "description": "CycleMind AI Cloud Functions — server-side Claude proxy.",
  "engines": { "node": "20" },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.30.1",
    "firebase-admin": "^12.3.0",
    "firebase-functions": "^5.0.0"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  },
  "private": true
}

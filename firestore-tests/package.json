{
  "name": "firestore-rules-tests",
  "private": true,
  "type": "module",
  "description": "Security-rules tests for corruption-reporting-system-final and bike-guide-app. Optional tooling — neither site depends on this to run.",
  "scripts": {
    "test": "npm run test:corruption && npm run test:bikeguide",
    "test:corruption": "firebase emulators:exec --project corruption-rules-test --only firestore \"node corruption.test.mjs\"",
    "test:bikeguide": "firebase emulators:exec --project bikeguide-rules-test --only firestore \"node bikeguide.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.0.0",
    "firebase-tools": "^14.0.0"
  }
}
